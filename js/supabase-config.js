// If you are deploying via Netlify with environment variables set,
// this file is overwritten automatically at build time — leave it alone.
//
// If you are drag-and-dropping this folder into Netlify with NO build step,
// paste your own values below between the quotes, then save.
window.SUPABASE_CONFIG = {
  url: "YOUR_SUPABASE_PROJECT_URL",
  anonKey: "YOUR_SUPABASE_ANON_KEY"
};
